set $mode_power [L]ock | [E]xit | [S]uspend | [P]oweroff
# Power menu
bindsym $mod+Escape mode "$mode_power"

mode "$mode_power" {
    bindsym l exec --no-startup-id ~/.config/i3/scripts/lock.sh, mode "default"
    bindsym e exec --no-startup-id i3-msg exit
    bindsym s exec --no-startup-id "~/.config/i3/scripts/lock.sh && systemctl suspend", mode "default"
    bindsym p exec --no-startup-id systemctl poweroff

    # Cancel
    bindsym Escape mode "default"
    bindsym Return mode "default"
}

set $mode_music Radio: [S]ympa | [L]ofi | [Q]uit radio
# Music menu
bindsym $mod+F3 mode "$mode_music"

mode "$mode_music" {
  bindsym l exec --no-startup-id ~/.config/i3/scripts/music.sh lofi, mode "default"
  bindsym s exec --no-startup-id ~/.config/i3/scripts/music.sh radio_sympa, mode "default"
  bindsym q exec --no-startup-id ~/.config/i3/scripts/music.sh stop, mode "default"

    # Cancel$
    bindsym Escape mode "default"
    bindsym Return mode "default"
}

# Resize mode
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window's width.
        # Pressing right will grow the window's width.
        # Pressing up will shrink the window's height.
        # Pressing down will grow the window's height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym m resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

# Resize mode
bindsym $mod+r mode "resize"
