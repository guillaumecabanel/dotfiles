# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock -c 000000 --nofork

# X screensaver triggers after 600s (10 min), which xss-lock will catch
exec --no-startup-id xset s 6000

# Monitor turns off after 660s (shortly after lock)
exec --no-startup-id xset dpms 6100 6100 6100

# clipboard history
exec --no-startup-id greenclip daemon
